<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amiiboroom.ordercollector.dao.UserDao">

    <insert id="I01" parameterType="hashmap">

    </insert>

    <select id="S01" parameterType="hashmap" resultType="hashmap">

    </select>

    <select id="S02" parameterType="string" resultType="hashmap">

    </select>

</mapper>